# Maaser Tracker (Flask)

A Flask web app to track income, exempt expenses, and maaser given.

## Quick start (Windows)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt  # or `pip install flask PyPDF2`
set FLASK_APP=app.py
flask run
